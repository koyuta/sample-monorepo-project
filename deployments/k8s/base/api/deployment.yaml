apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  selector:
    matchLabels:
      app: api
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: api
        env: local
    spec:
      containers:
        - name: api
          image: project_1/api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          command: ["/usr/local/bin/api"]
          env:
            - name: ENVIRONMENT
              value: set_env_name
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 12
            periodSeconds: 5
